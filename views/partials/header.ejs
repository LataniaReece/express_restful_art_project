<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Art Gallery</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
    <div id="top-alert" class="alert alert-primary d-none mb-0" role="alert">
        This is a primary alert with <a href="#" class="alert-link ml-1">an example link</a>. Give it a click if you like.<button type="button" id="exit-top-alert" class="btn btn-outline-primary ml-auto btn-sm">X</button>
      </div>

       <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4 wg-100" id="mainNavbar">
          <a class="navbar-brand mr-3" href="/">Amazing Art Gallery</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon my-toggle-menu"></span>
        </button>
      
        <div class="collapse navbar-collapse" id="navbarSupportedContent">

          <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
              <a class="nav-link" href="/paintings">Paintings Main</a>
            </li>
          </ul>
          <ul class="navbar-nav ml-auto">
            <% if(!currentUser){ %>
              <li class="nav-item mr-3">
                <span id="welcome-guest" class="nav-link" href="#">Welcome, Guest</span>
              </li>
            <li class="nav-item mr-3">
              <a class="nav-link" href="/login">Login</a>
            </li>
            <li class="nav-item mr-3" >
              <a class="nav-link" href="/register">Sign Up</a>
            </li>
            <% } else { %>
              <li class="nav-item mr-3">
                <span class="nav-link">Welcome, <a id="profile-link" href="/user/<%= currentUser._id %>"><%= capitlizeFirstLetter(currentUser.username) %></a></span>
              </li>
            <li class="nav-item mr-3" >
              <a class="nav-link" href="/logout">Sign Out</a>
            </li>
            <% } %>
        </ul> 
         
        </div>
      </nav>

      <div id="alert-container">
        
        <% if(error && error.length > 0){ %>
            <div class="alert alert-danger" role="alert">
                <%= error %>
            </div>
        <% } %>
        <% if(success && success.length > 0){ %>
            <div class="alert alert-success" role="alert">
                <%= success %>
            </div>
        <% } %>
        <% if(warning && warning.length > 0){ %>
          <div class="alert alert-warning" role="alert">
              <%= warning %>
          </div>
      <% } %>
    </div>